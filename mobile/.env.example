# Optional dev override for the backend base URL.
# In production the app defaults to https://www.toolsyourway.com (set in app.json extra.apiBaseUrl).
# Expo public env vars must be prefixed with EXPO_PUBLIC_ to be readable at runtime.
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:5000
